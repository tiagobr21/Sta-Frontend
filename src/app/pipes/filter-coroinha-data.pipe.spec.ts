import { FilterCoroinhaDataPipe } from './filter-coroinha-data.pipe';

describe('FilterCoroinhaDataPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterCoroinhaDataPipe();
    expect(pipe).toBeTruthy();
  });
});
