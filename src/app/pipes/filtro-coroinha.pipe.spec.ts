import { FiltroCoroinhaPipe } from './filtro-coroinha.pipe';

describe('FiltroCoroinhaPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroCoroinhaPipe();
    expect(pipe).toBeTruthy();
  });
});
